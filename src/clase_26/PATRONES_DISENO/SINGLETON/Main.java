package clase_26.PATRONES_DISENO.SINGLETON;

public class Main {

    public static void main(String[]args){

            //usando el patron singleton

            // => nombre de la clase + obtener la instancia interna + acceder a sus propieades y metodos
        if (JuegoHelper.getInstance().getDificultad() == JuegoHelper.getInstance().DIFICULTAD_BAJA){
            //hacer algo
        }
       
        
    }
}
